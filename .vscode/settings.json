{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "candy_shop",
            "database": "${workspaceFolder:pos-backend}/store.db"
        }
    ]
}